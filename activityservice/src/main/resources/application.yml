spring:
  application:
    name: activity-service
  config:
    import: "configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
  cloud:
    config:
      retry:
        max-attempts: 10
        initial-interval: 3000
        max-interval: 10000
      fail-fast: true
